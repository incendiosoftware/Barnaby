# Catalog orchestrator-related workspace changes
- Role: researcher
- Status: completed
- Attempts: 1
- Signal: Orchestrator integration changes are broad and active: main process now exposes `startOrchestratorGoalRun`/`startOrchestratorComparativeReview`/`getOrchestratorState`/`browseMarkdownFile`, replaces npm install/uninstall with `reloadLocalPlugins`, expands plugin discovery/loading, and adds async plugin host panel introspection; shared types and agent pipeline add role-oriented controls (`additionalSystemPrompt`, `toolRestrictions`) with tool allowlisting enforced in `agentTools` and propagated through OpenAI/OpenRouter/Codex clients; renderer wiring adds corresponding preload/API/types, new `OrchestratorPane` dashboard (goal-run/review modes, polled state, task/agent/log views, markdown import), plugin-host callback plumbing, and workspace/panel limits needed for multi-agent runs; workspace artifacts confirm live orchestration (`.barnaby/orchestrator/state.json` + `status.md`: goal run `mmkd70z7-2trv9j` running, 4 decomposed tasks, first researcher task in progress, 14 historical run dirs); deployment/workspace coupling is present via new `orch:build`/`orch:deploy` scripts, `build:all`, `electron-builder` `afterPack` hook, and workspace reference to `../barnaby-orchestrator`.
## Output
[SIGNAL:completed] Orchestrator integration changes are broad and active: main process now exposes `startOrchestratorGoalRun`/`startOrchestratorComparativeReview`/`getOrchestratorState`/`browseMarkdownFile`, replaces npm install/uninstall with `reloadLocalPlugins`, expands plugin discovery/loading, and adds async plugin host panel introspection; shared types and agent pipeline add role-oriented controls (`additionalSystemPrompt`, `toolRestrictions`) with tool allowlisting enforced in `agentTools` and propagated through OpenAI/OpenRouter/Codex clients; renderer wiring adds corresponding preload/API/types, new `OrchestratorPane` dashboard (goal-run/review modes, polled state, task/agent/log views, markdown import), plugin-host callback plumbing, and workspace/panel limits needed for multi-agent runs; workspace artifacts confirm live orchestration (`.barnaby/orchestrator/state.json` + `status.md`: goal run `mmkd70z7-2trv9j` running, 4 decomposed tasks, first researcher task in progress, 14 historical run dirs); deployment/workspace coupling is present via new `orch:build`/`orch:deploy` scripts, `build:all`, `electron-builder` `afterPack` hook, and workspace reference to `../barnaby-orchestrator`.