# Agent Orchestrator Notes

This file is retained for historical context from early development.

For the current and maintained project documentation, use:

- `README.md` (root)

Quick reminders:

- Run dev: `npm run dev`
- Build app: `npm run build:dist`
- Package installers/artifacts: `npm run build`

